package com.callvault.app.data.local.converter

/**
 * Room TypeConverters.
 *
 * All entity columns currently use Room-native primitives. Add converters
 * here when a non-primitive type (e.g. `kotlinx.datetime.Instant`) is
 * introduced.
 */
class Converters
